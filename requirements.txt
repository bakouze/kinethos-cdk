aws-cdk-lib==2.211.0
constructs>=10.0.0,<11.0.0

# API Gateway v2 (HTTP API) — now stable in aws-cdk-lib (no separate alpha needed)

aws-cdk.aws-lambda-python-alpha==2.211.0a0

# AWS SDK (useful in Lambda and for local scripts)
boto3>=1.34.0

# For the Telegram bot Lambda
python-telegram-bot==21.*
python-dotenv>=1.0.1